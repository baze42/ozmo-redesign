One-line: Labelled text field with terracotta focus ring and hint/error states.

```jsx
<Input label="Work email" placeholder="you@business.com" hint="We'll never share it." />
<Input label="Website" error="Please enter a valid URL." />
```

Focus shows a terracotta ring. Pass `error` to switch to the danger state; `hint` for helper text.